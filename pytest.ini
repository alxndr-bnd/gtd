[pytest]
testpaths = tests
pythonpath = .
addopts = -q
filterwarnings =
    # starlette советует httpx2 для TestClient; сам TestClient работает, ждём стабильного перехода
    ignore:Using `httpx` with `starlette.testclient` is deprecated
